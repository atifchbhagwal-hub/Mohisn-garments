# Mohsin Garments POS
Web POS (sale, stock with size/color, categories, receipt print). Runs on GitHub Pages with Firebase Firestore.
Setup: publish `firestore.rules` in Firebase console, enable Email/Password + Anonymous auth, enable GitHub Pages (main branch, root). 
